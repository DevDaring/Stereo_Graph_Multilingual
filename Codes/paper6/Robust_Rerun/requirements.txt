# Robust_Rerun - global install (no venv). Flash-attention is installed SEPARATELY
# from a pre-compiled wheel via install_flash_attention.py (Linux x86_64 + CUDA only).
torch>=2.3
transformers>=4.44
accelerate>=0.33
bitsandbytes>=0.43
networkx>=3.1
numpy>=1.24
pyyaml>=6.0
python-dotenv>=1.0
openai>=1.40
